#include <stdio.h>
#include <string.h>
int main()
{
    // all digits same is not allowed
    // where we are drawing line is inclusive
    char str [] = "0011";
    int len = strlen(str);
    int l = 0;
    int r = len-1;
    
    while (l<=r)
    {
        // scan to the left for number of zeros
        // scan to the right for number of ones
    }
    
    
    return 0;
}
